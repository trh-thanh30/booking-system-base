export const USER_ROLES = [
  "SUPER_ADMIN",
  "OWNER",
  "STAFF",
  "CUSTOMER",
] as const;

export const BUSINESS_USER_ROLES = ["OWNER", "STAFF"] as const;

export const USER_STATUSES = ["ACTIVE", "INACTIVE"] as const;
