export const TENANT_STATUSES = ["ACTIVE", "SUSPENDED", "DISABLED"] as const;

export const TENANT_DOMAIN_TYPES = [
  "SUBDOMAIN",
  "CUSTOM_DOMAIN",
  "INTERNAL",
] as const;

export const BUSINESS_STATUSES = ["ACTIVE", "SUSPENDED", "DISABLED"] as const;
