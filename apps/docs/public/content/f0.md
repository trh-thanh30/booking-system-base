# F0 - Core Infrastructure & SaaS Foundation

F0 thiết lập nền tảng SaaS multi-tenant tối thiểu: tenant database, tenant resolution, tenant context, health readiness/liveness và shared contract giữa API/FE.

## Endpoint đã triển khai

| Method | Endpoint                              | Mục đích                                     |
| ------ | ------------------------------------- | -------------------------------------------- |
| GET    | `/health/liveness`                    | Kiểm tra process API còn sống                |
| GET    | `/health/readiness`                   | Kiểm tra API sẵn sàng nhận traffic           |
| GET    | `/health/live`                        | Alias tương thích cũ cho liveness            |
| GET    | `/health`                             | Detailed health/readiness tương thích cũ     |
| GET    | `/tenants/resolve?host={host}`        | Resolve host/domain thành tenant             |
| GET    | `/internal/tenants/:tenantId/context` | Lấy tenant context đầy đủ cho internal/admin |

## Tenant resolution

Thứ tự resolve tenant trong middleware:

1. Header `x-tenant-id`
2. Header `x-tenant-host`
3. Header `host`

Nếu resolve được, API gắn tenant context vào request:

```ts
req.tenant = {
  id,
  slug,
  name,
  status,
  timezone,
  locale,
  settings,
  domains,
};
```

Route nào cần tenant có thể dùng `@RequireTenant()` và lấy context bằng `@Tenant()`.

## Seed tenant demo

Seed tạo tenant demo:

| Field    | Giá trị                                |
| -------- | -------------------------------------- |
| Slug     | `demo-spa`                             |
| Name     | `Demo Spa`                             |
| Status   | `ACTIVE`                               |
| Timezone | `Asia/Ho_Chi_Minh`                     |
| Locale   | `vi`                                   |
| Domains  | `demo.localhost`, `demo-spa.localhost` |

## Ví dụ resolve

```txt
GET /api/v1/tenants/resolve?host=demo.localhost
```

Response dùng shared `ApiResponse<T>`:

```json
{
  "success": true,
  "data": {
    "tenant": {
      "id": "...",
      "slug": "demo-spa",
      "name": "Demo Spa",
      "status": "ACTIVE",
      "timezone": "Asia/Ho_Chi_Minh",
      "locale": "vi"
    },
    "domain": {
      "id": "...",
      "host": "demo.localhost",
      "type": "SUBDOMAIN",
      "is_primary": true
    }
  },
  "message": "Tenant resolved successfully",
  "meta": {
    "timestamp": "...",
    "version": "v1",
    "requestId": "..."
  }
}
```

## Database F0

Các bảng nền tảng:

- `tenant`
- `tenant_domain`
- `tenant_settings`
- `job_run`
- `system_event`

Các bảng hiện có được thêm liên kết tenant optional để không phá dữ liệu cũ:

- `user.tenant_id`
- `asset.tenant_id`
- `notification.tenant_id`
- `activity_log.tenant_id`
