# =========================================================================
# PLATFORM ADMIN PORTAL
# =========================================================================
# Copy into apps/platform-admin/.env.local only when this app needs app-local overrides.
# Root .env.development is still the default for monorepo scripts.

PLATFORM_ADMIN_PORT=3003
NEXT_PUBLIC_PLATFORM_ADMIN_URL="http://localhost:3003"
NEXT_PUBLIC_API_URL="http://localhost:3000/api/v1"
NEXT_PUBLIC_ASSET_URL="http://localhost:3000/uploads/public/"
NEXT_PUBLIC_AUTH_CONTEXT=platform

