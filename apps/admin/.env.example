# =========================================================================
# BUSINESS ADMIN PORTAL
# =========================================================================
# Copy into apps/admin/.env.local only when this app needs app-local overrides.
# Root .env.development is still the default for monorepo scripts.

ADMIN_PORT=3002
NEXT_PUBLIC_ADMIN_URL="http://localhost:3002"
NEXT_PUBLIC_API_URL="http://localhost:3000/api/v1"
NEXT_PUBLIC_ASSET_URL="http://localhost:3000/uploads/public/"
NEXT_PUBLIC_AUTH_CONTEXT=admin

